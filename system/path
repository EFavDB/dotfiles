# Add path (Miniconda3 3.19.0)
# With this path order, calls to python will default to the
# Miniconda version.

export PATH="/home/$USER/miniconda3/bin:$PATH"

# set PATH so it includes user's private bin if it exists
if [ -d "$HOME/bin" ] ; then
    PATH="$HOME/bin:$PATH"
fi